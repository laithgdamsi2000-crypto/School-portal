# دليل النشر — بدون سطر أوامر، كل شيء عبر المتصفح

هذا الدليل مصمم خصيصاً لمن لم يستخدم الطرفية (Terminal) من قبل. كل خطوة هنا
تتم بالنقر داخل المتصفح — لا حاجة لكتابة أي أمر برمجي.

**الوقت المتوقع: 30-40 دقيقة في أول مرة.**

---

## الخطوات باختصار

1. إنشاء حساب GitHub ورفع الملفات (سحب وإفلات)
2. إنشاء قاعدة بيانات مجانية على Neon
3. نشر المشروع على Vercel وربطه بـ GitHub
4. ربط تخزين Vercel Blob (مطلوب لرفع الملفات — صور، PDF، Word، Excel)
5. زيارة رابط إعداد تلقائي لإنشاء الصفوف وحساب المدير
6. تسجيل الدخول وتغيير كلمة المرور
7. حذف رابط الإعداد (خطوة أمان مهمة)

---

## 1. إنشاء حساب GitHub ورفع الملفات

- اذهب إلى [github.com](https://github.com) واضغط **Sign up** لإنشاء حساب
  جديد (بريد إلكتروني + كلمة مرور، مثل أي حساب آخر).
- بعد تسجيل الدخول، اضغط الزر الأخضر **New** (أو **Create repository**).
- اكتب اسماً مثل `school-portal`، اختر **Private**، ثم اضغط **Create repository**.
- في الصفحة التالية، ابحث عن رابط بعنوان **uploading an existing file**
  (أو **Add file → Upload files** من القائمة أعلى الصفحة).
- افتح مجلد المشروع الذي حمّلته (`school-portal.zip` بعد فك الضغط عنه) على
  جهازك، وقم بسحب **جميع الملفات والمجلدات بداخله** (وليس المجلد نفسه) وإفلاتها
  في صفحة GitHub.
- انتظر حتى ينتهي الرفع، ثم اضغط **Commit changes** أسفل الصفحة.

**ملاحظة:** إن رفض GitHub رفع مجلد `node_modules` أو ملف `.env` — هذا متوقع
وصحيح، فهذه الملفات لا يجب رفعها أبداً (ملف `.gitignore` المُرفق في المشروع
يمنع ذلك تلقائياً عند استخدام أدوات أخرى، لكن الرفع اليدوي هنا لا يقرأه، لذا
تأكد يدوياً أنك لم تسحب ملف `.env` أو مجلد `node_modules` إن وُجدا).

---

## 2. إنشاء قاعدة بيانات على Neon

- اذهب إلى [neon.tech](https://neon.tech) واضغط **Sign up** (يمكنك التسجيل
  مباشرة بحساب GitHub الذي أنشأته للتو).
- اضغط **Create a project**. اختر اسماً واختر أقرب منطقة جغرافية.
- بعد الإنشاء، ستظهر لك صفحة فيها **Connection string** يبدأ بـ
  `postgresql://...` — اضغط على أيقونة النسخ بجانبه واحتفظ به، ستحتاجه في
  الخطوة القادمة مباشرة.

---

## 3. نشر المشروع على Vercel

- اذهب إلى [vercel.com](https://vercel.com) واضغط **Sign up** — اختر
  **Continue with GitHub** لتسجيل الدخول بنفس حساب GitHub.
- بعد الدخول، اضغط **Add New → Project**.
- ستظهر قائمة بمستودعاتك (Repositories) — اختر `school-portal` الذي أنشأته،
  ثم اضغط **Import**.
- Vercel سيتعرف تلقائياً أن هذا مشروع Next.js. **لا تضغط Deploy بعد** —
  انزل لقسم **Environment Variables** في نفس الصفحة وأضف كل مما يلي (اسم
  المتغير في العمود الأول، والقيمة في العمود الثاني):

| الاسم (Name) | القيمة (Value) |
|---|---|
| `DATABASE_URL` | الرابط الذي نسخته من Neon في الخطوة 2 |
| `NEXTAUTH_SECRET` | أي نص عشوائي طويل (30+ حرفاً) — يمكنك كتابة أي جملة عشوائية من حروف وأرقام، مثل `xY9k2mP7qR4vN8wL1tZ6sB3dF5hJ0` |
| `NEXTAUTH_URL` | سيكون هذا رابط موقعك — اتركه فارغاً الآن، سنعود لتعبئته بعد أول نشر |
| `SETUP_SECRET_KEY` | كلمة سر مؤقتة من اختيارك، لأجل خطوة الإعداد القادمة فقط — مثل `MySetup2026Key` |
| `SEED_ADMIN_EMAIL` | البريد الإلكتروني الذي تريد تسجيل الدخول به كمدير |
| `SEED_ADMIN_PASSWORD` | كلمة المرور الحقيقية التي تريدها لحساب المدير |

- بعد إضافة الجميع، اضغط **Deploy**. سيأخذ أول بناء بضع دقائق — ستشاهد شريط
  تقدّم مباشرة على الشاشة.

---

## 4. ربط تخزين Vercel Blob (مطلوب لرفع الملفات)

بدون هذه الخطوة، أي محاولة لرفع ملف (مرفق واجب، مرفق إعلان، جدول حصص، أو
ملف عام من "إدارة الملفات") ستفشل برسالة خطأ — الموقع يحتاج مكاناً دائماً
لتخزين الملفات المرفوعة، ولا يمكنه الكتابة على نفس الخادم.

- من لوحة تحكم مشروعك على Vercel، اذهب إلى تبويب **Storage**.
- اضغط **Create Database** → اختر **Blob** → اختر اسماً ثم **Create**.
- في الخطوة التالية اختر **Connect to Project** وحدد مشروع `school-portal`.
- Vercel سيضيف متغير البيئة `BLOB_READ_WRITE_TOKEN` تلقائياً — لا حاجة
  لنسخ أي قيمة يدوياً.
- اذهب إلى تبويب **Deployments** واضغط **Redeploy** على آخر نشر حتى يُطبَّق
  المتغير الجديد.

---

## 5. تعبئة NEXTAUTH_URL بعد أول نشر

- بعد نجاح النشر، سيعطيك Vercel رابطاً مثل
  `https://school-portal-xxxx.vercel.app` — انسخه.
- ارجع إلى إعدادات المشروع على Vercel → **Settings → Environment Variables** →
  عدّل `NEXTAUTH_URL` وضع فيه هذا الرابط بالضبط.
- اذهب إلى تبويب **Deployments** واضغط **Redeploy** على آخر نشر، حتى يُطبَّق
  التغيير.

---

## 6. زيارة رابط الإعداد التلقائي

هذه الخطوة تنشئ الصفوف الاثني عشر، المواد الدراسية، وحساب المدير — تلقائياً،
بدون أي أمر برمجي.

- افتح المتصفح واذهب إلى:
  ```
  https://[رابط موقعك]/api/setup?key=[القيمة التي كتبتها في SETUP_SECRET_KEY]
  ```
  مثال: `https://school-portal-xxxx.vercel.app/api/setup?key=MySetup2026Key`

- ستظهر صفحة تأكيد باللغة العربية إذا نجحت العملية.

---

## 7. تسجيل الدخول وحذف رابط الإعداد

- اذهب إلى `https://[رابط موقعك]/admin/login` وسجّل الدخول بالبريد وكلمة
  المرور اللذين وضعتهما في `SEED_ADMIN_EMAIL` و `SEED_ADMIN_PASSWORD`.
- **مهم جداً — خطوة أمان لا تتجاهلها:** ارجع إلى إعدادات المشروع على Vercel
  واحذف متغير `SETUP_SECRET_KEY` نهائياً (اضغط على أيقونة الحذف بجانبه)، ثم
  أعد النشر (Redeploy) مرة أخرى. هذا يمنع أي شخص آخر من زيارة رابط الإعداد
  لاحقاً ومحاولة العبث بحساب المدير.

---

## بعد ذلك — كيف أضيف واجباً أو أعدّل شيئاً في الموقع؟

- **إضافة/تعديل محتوى (واجبات، إعلانات):** كل هذا يتم من داخل لوحة تحكم
  الإدارة على الموقع نفسه (`/admin`) — لا حاجة لأي شيء تقني بعد الآن.
- **تعديل كود أو تصميم (مثل معلومات صفحة "تواصل معنا"):** عدّل الملف مباشرة
  على GitHub (كل ملف فيه زر تعديل ✏️ بشكل مباشر من المتصفح)، أو أرسل لي طلب
  التعديل وسأجهز لك الملف المحدَّث لرفعه.
- **كل تعديل تحفظه على GitHub، Vercel ينشره تلقائياً خلال دقيقة أو دقيقتين** —
  لا حاجة لإعادة تكرار خطوات النشر من جديد.

---

## ملاحظات مهمة

- **رفع الملفات يعتمد على Vercel Blob** (الخطوة 4 أعلاه) — إن لم تُربط،
  ستفشل جميع محاولات رفع الملفات (مرفقات، جداول حصص، ملفات عامة) برسالة
  خطأ واضحة بدلاً من الفشل الصامت.
- **النسخ الاحتياطي لقاعدة البيانات:** Neon يحتفظ بنسخ احتياطية تلقائية على
  الخطة المجانية لعدة أيام — يمكن ضبط ذلك من إعدادات المشروع هناك.
